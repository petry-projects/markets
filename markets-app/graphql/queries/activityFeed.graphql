query ActivityFeed($limit: Int, $offset: Int) {
  activityFeed(limit: $limit, offset: $offset) {
    id
    actorID
    actionType
    targetType
    targetID
    marketID
    message
    createdAt
  }
}
