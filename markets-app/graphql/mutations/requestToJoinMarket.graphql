mutation RequestToJoinMarket($marketID: ID!, $dates: [String!]!, $acknowledgeRules: Boolean!) {
  requestToJoinMarket(marketID: $marketID, dates: $dates, acknowledgeRules: $acknowledgeRules) {
    id
    marketID
    vendorID
    status
    date
    rulesAcknowledged
    createdAt
  }
}
